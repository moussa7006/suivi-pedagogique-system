/**
 * Zone.js flags must be declared here before importing zone.js.
 *
 * Ionic relies heavily on Web Components, so do not disable the
 * customElements patch here. Disabling it can prevent Angular change
 * detection from running after Ionic/Web Component callbacks.
 */
